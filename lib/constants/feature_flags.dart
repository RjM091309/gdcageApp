/// Notifications (bell icon, badge, toast popup, background check) are temporarily disabled —
/// flip back to true to restore. Gates: header bell icon/badge, in-app poll timer, toast popup,
/// and the Android background WorkManager notification check.
const bool kNotificationsEnabled = false;
